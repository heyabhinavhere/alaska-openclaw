# Watchers V1 Plan — Reconciliation Against Current Main (2026-05-30)

> Read-only reconciliation. Every claim is backed by `git show origin/main:<path>` (fetched `dcda1c7`). Plan under review: `docs/superpowers/plans/2026-05-27-alaska-watchers-v1.md` (968 lines). Spec: `docs/superpowers/specs/2026-05-26-alaska-watchers-v1.md`.

## Summary

The plan is **mostly structurally sound but factually stale in ~9 places** — it was authored on the `feat/watchers-v1-plan` line, which the live-state research confirms was 33 commits behind production and missing the entire `user-profile-360` skill + migration 0003 + workspace persistence. The most damaging staleness is **migration numbering** (plan reuses `0003`, which is taken) and **two skill-interface assumptions that no longer hold** (the spec's worked examples invoke a never-built `identity-resolver` skill; the real identity/email-resolution path is now `user-profile-360`). There are **2 BLOCKER deltas** (migration collision, KB-dependency-not-in-repo) and 1 near-blocker (a second migration `0004` collision at hard-cut). Net: a focused ~1-hour revision pass fixes it; no re-architecture needed.

## Deltas (each = a concrete plan edit)

### Delta 1: Watchers migration must be `0004`, not `0003`
- **Plan currently says:** Task W0.1 — "Migration 0003 — watchers + watcher_fires tables" → `Create: migrations/0003_watchers_v1.sql`. Repeated in W3.2 Step 2 ("Update `migrations/0003_watchers_v1.sql` to add … event_pollers"), W4.2/Verification Plan, and the commit messages.
- **Current main reality:** `git show origin/main:migrations/` lists `0001_v2_task_model.sql`, `0002_classifier_secondary_intents.sql`, **`0003_user_profile_360.sql`**, `run_migrations.sh`. `0003` is taken by Sandeep's 360 skill (and `git show origin/main:docs/superpowers/research/2026-05-30-post-incident-integrity-audit.md` confirms `_migrations` has all three recorded, `0003` applied live 2026-05-29).
- **Required change:** Rename the watchers migration to `0004_watchers_v1.sql` everywhere it appears (W0.1 file path + 3 verify commands + commit msg; W3.2 Step 2 `event_pollers` add; W4.2 + Verification Plan static checks). The W0.1 Step 2 local-verify must also apply `0003_user_profile_360.sql` before `0004` in the test chain.
- **Severity:** BLOCKER (the file would still run since `run_migrations.sh` globs lexically, but the *plan's own commands* — `sqlite3 ... < 0003_watchers_v1.sql` and the static check `grep` — reference a filename that collides with the live 360 migration; building to the plan literally creates `0003_watchers_v1.sql` shadowing/clashing with the real `0003`).

### Delta 2: Hard-cut "drop Phase C tables" migration also wants `0004` — bump to `0005`
- **Plan currently says:** W4.3 Step 3 — "Drop `scheduled_actions` and `routine_proposals` tables (migration 0004)".
- **Current main reality:** With Delta 1, `0004` is now the watchers migration. The next free number is `0005`. (`scheduled_actions` and `routine_proposals` both live in `git show origin/main:migrations/0001_v2_task_model.sql` — lines 142 and 172 — so the drop is a real future migration, not a no-op.)
- **Required change:** Renumber the hard-cut drop migration to `0005_drop_phase_c.sql` in W4.3 Step 3.
- **Severity:** IMPORTANT (downstream of Delta 1; would otherwise double-assign `0004`).

### Delta 3: Worked examples invoke `identity-resolver` (never built) — should be `user-profile-360`
- **Plan currently says:** W4.2 replays "the 5 scenarios in the spec's Worked examples §" verbatim. The spec's action-chain DSL example and Example 1 (weekly card-linkage) chain `{"step":"invoke_skill","skill":"identity-resolver","args":{"user_ids":"..."}}` to resolve failed-user emails; Example 2 (gift-card emails) assumes a CIO email-resolution step.
- **Current main reality:** `git ls-tree origin/main:skills/` has **no `identity-resolver`** (grep returns nothing). The 22 skills include `user-profile-360`. `git show origin/main:skills/user-profile-360/SKILL.md` is the real per-user lookup: one command `python3 /data/skills/user-profile-360/lookup.py --query <v> --query-type <user_id|email|phone|name> --intent <...> --requester-slack-id <id> --requester-authority <admin|founder|engineer> --channel-type <dm|channel>`, returns JSON on stdout, caches in 4 tables. The 360 skill resolves identity AND returns email/profile in one shot. The live-state research (`2026-05-27-live-alaska-state-and-360-profile.md`, "For Watchers V1") explicitly calls it "a strong new `invoke_skill` target."
- **Required change:** Update W1.1/W2.1's `invoke_skill` step documentation and W4.2's replay expectations so email/identity resolution uses `user-profile-360` (`lookup.py`, `--query-type email|user_id`, `--intent user_summary` for cheap email-only resolution, `full_picture`/`credit_health` for enriched alerts). Note in the plan that `identity-resolver` does not exist — the action-chain step in the spec's DSL example is aspirational. (The spec itself references `user-profile-360` zero times — see also New-tasks below.)
- **Severity:** IMPORTANT (the build would wire a non-existent skill; the replay would fail at the resolution step).

### Delta 4: KB Tier-1 dependency files are NOT in the repo on main — pre-flight P.2 will fail
- **Plan currently says:** Pre-flight P.2 — `ls workspace/knowledge/integrations/plaid.md` … "Expected: all files present. If KB Tier 1 is incomplete, STOP." Dependencies list marks BON KB Tier 1 as "⏳ Abhinav seeding in parallel — REQUIRED before Phase W.1."
- **Current main reality:** `workspace/knowledge/` **does not exist in git on main** — the post-incident audit (Section 4) states "no `workspace/knowledge/` in the git image → nothing to seed," and the live-state research confirms the KB is "entirely local + undeployed (~25 files Abhinav is seeding)" and untracked. `git show origin/main:docs/superpowers/specs/2026-05-26-bon-knowledge-base.md` lines 289-304 list the 13 Tier-1 files the plan depends on, but they are spec'd, not committed. `sync_workspace.sh` lists `knowledge` in `WS_CONFIG_DIRS` so it *would* deploy once committed — but it isn't yet.
- **Required change:** Make the plan's first dependency an explicit, gating task: "Commit BON KB Tier-1 files to the deployed branch line (they are currently untracked/local). P.2's `ls` checks run against the repo, not Abhinav's laptop." Until the KB is committed to main, W.1 cannot start — restate this as a hard prerequisite with a verification that the files are *git-tracked*, not just present on disk.
- **Severity:** BLOCKER (W.1 depends on KB; the KB is not in the repo, so a clean main checkout has none of these files and P.2 stops the build).

### Delta 5: Add a KB doc for the admin profile API (`integrations/user-profile-api.md`) to the KB dependency
- **Plan currently says:** W1.1 LOAD RELEVANT KB step keyword-maps "user" → `data-models/user.md`, "credit score" → `data-models/credit-profile.md`. No mention of the 360 admin-API surface.
- **Current main reality:** The live-state research (Q3, "For the BON Knowledge Base") finds `user.md`/`credit-profile.md` are written from BON's *Postgres schema* POV, while `user-profile-360` consumes a *different, higher surface* (Sandeep's `/api/admin/users/{id}/profile` with named sections per `sections.py`). It recommends adding `integrations/user-profile-api.md` and cross-linking. The KB has no doc for the admin profile API today.
- **Required change:** Add `integrations/user-profile-api.md` to the Tier-1 KB list the plan gates on (or at minimum to W1.1's keyword map: "user / profile / credit band / debt → integrations/user-profile-api.md"), so watcher-creator drafts that enrich via `lookup.py` load the right contract (endpoints, `X-Admin-Key`, `BON_API_BASE_URL`/`BON_ADMIN_API_KEY`, section catalog, intents).
- **Severity:** MINOR (improves correctness of drafted watchers; not build-blocking).

### Delta 6: "update MEMORY.md" for history → redirect to `memory/system-evolution.md`
- **Plan currently says:** The plan doesn't directly write history to MEMORY.md, but its activation/handoff sections (W4.3 Step 2-3) describe DMing Abhinav and logging the dual-write window; any "record this in MEMORY.md" framing the executor adds for evolution history is now wrong.
- **Current main reality:** `git show origin/main:workspace/MEMORY.md` is now a **lean, always-injected core** (roster + identity + data-source IDs). It explicitly points history to `memory/system-evolution.md` ("Sprint Board retired — see `memory/system-evolution.md` → v2.2"). Tiered memory per project state: core in MEMORY.md, history in `memory/system-evolution.md`, daily detail in `memory/YYYY-MM-DD.md`.
- **Required change:** Add a one-line note to the plan's Execution Handoff: "Watcher-system milestones / dual-write window / hard-cut go in `memory/system-evolution.md`, NOT MEMORY.md. MEMORY.md is lean always-injected core — only the team roster + Notion IDs + data-source IDs live there."
- **Severity:** MINOR (cosmetic/process; prevents bloating the always-injected core).

### Delta 7: Janitor + event-poller cron JSON shape doesn't match the live `jobs.json` schema
- **Plan currently says:** W4.1 janitor cron entry uses `"payload": {"kind": "user-message", "message": "..."}`, `"delivery": {"mode": "none"}`, `"schedule": {"kind": "cron", "expr": "0 4 * * *", "tz": "UTC"}`. W3.2 adds 4 event-poller crons with "Payload message: 'Run event-poller …'".
- **Current main reality:** `git show origin/main:config/cron-jobs-backup.json` is `{version, jobs:[...]}` (14 entries). Each job's real shape: `{id, agentId:"main", sessionKey:"agent:main:main", name, enabled, createdAtMs, updatedAtMs, schedule:{kind:"cron",expr,tz}, sessionTarget:"isolated", wakeMode:"now", payload:{kind:"agentTurn", message:"..."}}`. The payload kind is **`agentTurn`**, not `user-message`, and there is **no `delivery` field** in the live entries.
- **Required change:** In W3.2 and W4.1, change the example cron JSON to match the live `jobs.json` schema: `payload.kind = "agentTurn"`, add `sessionTarget`, `wakeMode`, `agentId`, `sessionKey`; drop the `delivery` block (delivery suppression is handled by the skill posting its own Slack messages, per the existing "delivery.channel=none" pattern the spec §"OpenClaw deeper research" Q3 references). Verify against `config/cron-jobs-backup.json` before writing.
- **Severity:** IMPORTANT (cron entries built to the plan's shape may not register / may mis-deliver).

### Delta 8: Cron-count + concurrency math — live count is 13, not ~14
- **Plan currently says:** Spec schema comment + plan rely on "30 watchers all firing at `0 9 * * *` against `maxConcurrentRuns=8` would queue" for the stagger justification; the plan's per-watcher-cron scaling assumes headroom from a ~14 baseline.
- **Current main reality:** The post-incident audit (Section 5) confirms live `jobs.json` = **13 jobs, all enabled** (a strict superset of the pre-v5.26 backup's 12). CLAUDE.md's "~14" and the backup file's 14 entries are approximations; the live runtime carries 13. The stagger logic (`stagger_seconds` 0-300) is sound and unchanged.
- **Required change:** Update the plan's scaling note to baseline **13 live crons** (4 event-poller crons + 1 janitor + N per-watcher crons land on top of 13). Re-affirm `maxConcurrentRuns=8` is the constraint stagger protects. No schema change — just correct the starting number.
- **Severity:** MINOR (sanity-check number; stagger design already handles it).

### Delta 9: Intent-classifier is v1.1.0 with 9 intents — the WATCHER_REQUEST add is correctly layered, but pin the version/commit messages
- **Plan currently says:** W0.2 — "The classifier currently has 9 intents. Add a 10th: WATCHER_REQUEST"; "Bump version from `1.1.0` → `1.2.0`"; commit msg reads "v1.1.0 → v1.2.0". Disambiguation rule slots into "the 'Disambiguation rules' subsection (added in v1.1)".
- **Current main reality:** `git show origin/main:skills/intent-classifier/SKILL.md` is **`version: 1.1.0`** with exactly **9 intents** (TASK_CREATE/UPDATE/ASSIGN/BLOCKER, REMINDER_REQUEST, DECISION_RECORDED, STATUS_QUERY, NON_WORK_CHAT, AMBIGUOUS) and a populated "Disambiguation rules (v1.1 — tuned from May 18-24 replay findings)" section with META-COMMENTS / STANDUP CONTEXT / SHARING-vs-ASSIGNING / MULTI-INTENT rules. The plan's assumption matches main exactly.
- **Required change:** Mostly NONE — this delta is a *confirmation* the plan is correct here. Only nit: the W0.2 commit-message header says "feat(classifier): add WATCHER_REQUEST intent (v1.1.0 → v1.2.0)" — keep that; ensure the new REMINDER_REQUEST-vs-WATCHER_REQUEST disambiguation rule is appended to the *existing* v1.1 rules block (don't recreate it) and that the prompt's hard-coded "ONE of these intent types: …" enum (currently 9 names, no WATCHER_REQUEST) gets the 10th name added in both the table AND the inline prompt enum.
- **Severity:** MINOR (plan is right; flag only so the executor edits the live v1.1 prompt enum, not a remembered older one).

## Tasks now MOOT

- **None fully MOOT.** No watchers task was pre-implemented on main (`git ls-tree origin/main:skills/` has no `watcher-creator`, `watcher-dispatcher`, `event-poller`, or `watcher-janitor`; no `0004`+ migration exists). The watchers feature is entirely unbuilt on main.
- **Partially obviated:** W0.1 Step 3 ("Confirm migration runner picks it up — no code change needed") is still **correct** — `git show origin/main:migrations/run_migrations.sh` uses `shopt -s nullglob` + lexical glob + a `_migrations` tracking table, and is invoked positionally as `bash /opt/migrations/run_migrations.sh /data/queue/alaska.db /opt/migrations` from `entrypoint.sh`. The runner is more sophisticated than the plan implies (atomic per-migration transaction + tracking insert) but needs no change for `0004`.

## New tasks/dependencies to ADD

1. **ADD dependency: `user-profile-360` as an `invoke_skill` target.** The plan's W1.1/W2.1 action-chain step catalog should list `user-profile-360` (via `lookup.py`) as the canonical identity→email→profile resolver, replacing the phantom `identity-resolver`. Add to W2.1 a note: watcher fires that enrich a user use the narrowest `--intent` (e.g. `user_summary` for email-only) to keep cost down; repeated enrichment of the same user hits `user_profile_cache`. (See Delta 3.)
2. **ADD task: commit the BON KB to the deployed branch line** before W.1 (currently untracked/undeployed). Gate it with a *git-tracked* verification, not a disk `ls`. (See Delta 4.)
3. **ADD KB file: `integrations/user-profile-api.md`** to the Tier-1 list and W1.1 keyword map. (See Delta 5.)
4. **ADD note: branch off `main`, not the stale plan branch.** The plan's Pre-flight P.1 says `git checkout main && git pull`. Reaffirm this explicitly — the live-state research warns that building/deploying from `feat/watchers-v1-plan` would regress production (delete `user-profile-360`, revert workspace persistence). The plan's P.1 is correct; just make the warning loud.
5. **ADD opportunity: watcher Notion writes can now set Owner (people).** `git show origin/main:workspace/MEMORY.md` line 79: "Owner (people) field writes are now ENABLED — set Owner with the roster Notion ID `{"people":[{"id":"..."}]}`." Any watcher action that creates a Notion entry (e.g. a `create_task`-style write surfacing to Notion, or future bug/risk-register writes) should populate Owner from the roster Notion User ID. Add a line to W2.1's `create_task` / Notion-write step semantics. (Note: Sprint Board writes remain paused entirely per MEMORY.md — don't target it.)
6. **ADD note: Phase C migration entrypoint edit is an ADD, not a conflict.** W2.3 Step 3 inserts a one-time `migrate_phase_c_to_watchers.py --execute` guarded by `/data/.openclaw/.phase_c_migrated`. `git show origin/main:entrypoint.sh` has the migration-runner block (`bash /opt/migrations/run_migrations.sh /data/queue/alaska.db /opt/migrations`) but no Phase C Python hook — so the plan's insertion is clean. Confirm the insertion lands *after* the migration-runner block and *before* `exec openclaw gateway run`, and that `PYTHONPATH=/opt/lib` (already exported at the top of entrypoint) covers the script's imports.

## Net assessment

**Mostly-fine-with-tweaks — a single focused revision pass (~1 hour), not a rewrite.** The architecture (per-watcher cron, `watchers`/`watcher_fires` tables, action-chain DSL, two-stage approval, memory model, janitor) is fully compatible with current main and nothing on main pre-empts it. The blockers are mechanical: (1) renumber the migration `0003→0004` (and the hard-cut `0004→0005`) everywhere, and (2) treat the BON KB as an un-met, not-yet-in-repo dependency that gates W.1. The two interface corrections (`identity-resolver`→`user-profile-360`, cron JSON shape→`agentTurn`) prevent the executor from wiring non-existent surfaces. Apply Deltas 1-4 and 7 before execution; Deltas 5, 6, 8, 9 are polish that can ride along. After these edits the plan is execution-ready against `origin/main`.
