# Self-PR spike (throwaway)

Created by `lib/open_self_pr.py` to validate that Alaska can open a PR from the
container via the GitHub REST API (the container has no git and no gh CLI).

This PR is closed and its branch deleted immediately after verification.
Safe to ignore.
