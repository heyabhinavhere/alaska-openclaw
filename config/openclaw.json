{
  "gateway": {
    "port": 18789,
    "bind": "lan",
    "mode": "local",
    "trustedProxies": ["0.0.0.0/0"],
    "auth": {
      "mode": "token"
    },
    "controlUi": {
      "allowedOrigins": ["https://alaska-openclaw-production.up.railway.app"],
      "dangerouslyDisableDeviceAuth": true
    }
  },
  "channels": {
    "slack": {
      "enabled": true,
      "dmPolicy": "open",
      "allowFrom": ["*"],
      "groupPolicy": "open",
      "streaming": false,
      "nativeStreaming": false
    }
  },
  "tools": {
    "agentToAgent": {
      "enabled": true
    }
  },
  "hooks": {
    "enabled": true,
    "token": "__HOOKS_TOKEN__",
    "path": "/hooks",
    "mappings": [
      {
        "path": "fireflies",
        "action": "agent",
        "message": "Fireflies webhook received. A new meeting transcript is ready. Meeting ID: {{meetingId}}, Event: {{eventType}}. Use the Fireflies API to fetch the full transcript, then extract decisions, action items, blockers, and open questions. Write the structured meeting note to the Meeting Notes database in Notion.",
        "name": "Meeting Intelligence",
        "deliver": true,
        "channel": "last"
      }
    ]
  }
}
